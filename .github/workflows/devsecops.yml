name: DevSecOps Pipeline

"on":
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  sast-bandit:
    name: SAST - Bandit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Bandit
        run: pip install bandit

      - name: Run Bandit scan
        run: bandit -r test-app/ -f json -o reports/bandit_report.json || true

      - name: Upload Bandit report
        uses: actions/upload-artifact@v4
        with:
          name: bandit-report
          path: reports/bandit_report.json

  sast-semgrep:
    name: SAST - Semgrep
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Semgrep
        run: pip install semgrep

      - name: Run Semgrep scan
        run: |
          semgrep --config=p/python --json \
            --output reports/semgrep_report.json \
            test-app/ || true

      - name: Upload Semgrep report
        uses: actions/upload-artifact@v4
        with:
          name: semgrep-report
          path: reports/semgrep_report.json

  dast-zap:
    name: DAST - OWASP ZAP
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Flask
        run: pip install flask==3.1.1

      - name: Start Flask app
        run: |
          cd test-app
          nohup python app.py > /tmp/flask.log 2>&1 &
          echo "Waiting for Flask to start..."
          for i in {1..15}; do
            if curl -s http://localhost:5000 > /dev/null; then
              echo "Flask is up after ${i}s"
              break
            fi
            sleep 1
          done
          curl -v http://localhost:5000 || (cat /tmp/flask.log && exit 1)

      - name: Create reports directory
        run: mkdir -p reports

      - name: Run ZAP baseline scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          target: "http://localhost:5000"
          fail_action: false
          rules_file_name: ".zap/rules.tsv"
          cmd_options: "-J zap_report.json"
        continue-on-error: true

      - name: Move ZAP report to reports folder
        if: always()
        run: |
          if [ -f zap_report.json ]; then
            mv zap_report.json reports/zap_report.json
            echo "ZAP report moved successfully"
          else
            echo "No ZAP report found — creating empty placeholder"
            echo '{"site":[]}' > reports/zap_report.json
          fi

      - name: Upload ZAP report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: reports/zap_report.json

      - name: Upload ZAP report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: zap-report
          path: reports/zap_report.json

  sca-pip-audit:
    name: SCA - pip-audit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Run pip-audit scan
        run: pip-audit -r test-app/requirements.txt --format json -o reports/pip_audit_report.json || true

      - name: Upload pip-audit report
        uses: actions/upload-artifact@v4
        with:
          name: pip-audit-report
          path: reports/pip_audit_report.json

  container-scan-trivy:
    name: Container Scan - Trivy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Trivy config scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: config
          scan-ref: .
          format: json
          output: reports/trivy_report.json
          exit-code: "0"

      - name: Upload Trivy report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: reports/trivy_report.json

  secrets-scan-gitleaks:
    name: Secrets Scan - Gitleaks
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Gitleaks
        run: |
          docker run --rm -v "$PWD":/repo ghcr.io/gitleaks/gitleaks:latest \
            detect --source /repo --report-format json \
            --report-path /repo/reports/gitleaks_report.json \
            --exit-code 0 || true

      - name: Upload Gitleaks report
        uses: actions/upload-artifact@v4
        with:
          name: gitleaks-report
          path: reports/gitleaks_report.json

  security-gate:
    name: Security Gate
    runs-on: ubuntu-latest
    needs: [sast-bandit, sast-semgrep, sca-pip-audit, container-scan-trivy, secrets-scan-gitleaks]
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Bandit report
        uses: actions/download-artifact@v4
        with:
          name: bandit-report
          path: reports/

      - name: Download Semgrep report
        uses: actions/download-artifact@v4
        with:
          name: semgrep-report
          path: reports/

      - name: Download pip-audit report
        uses: actions/download-artifact@v4
        with:
          name: pip-audit-report
          path: reports/
        continue-on-error: true

      - name: Download Trivy report
        uses: actions/download-artifact@v4
        with:
          name: trivy-report
          path: reports/
        continue-on-error: true

      - name: Download Gitleaks report
        uses: actions/download-artifact@v4
        with:
          name: gitleaks-report
          path: reports/
        continue-on-error: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run aggregation and security gate
        id: gate
        run: python scripts/aggregate_results.py
        continue-on-error: true

      - name: Upload unified findings
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unified-findings
          path: reports/findings.json

      - name: Enforce security gate result
        if: always()
        run: |
          if [ "${{ steps.gate.outcome }}" == "failure" ]; then
            echo "Security gate failed — HIGH/CRITICAL findings detected. Blocking build."
            exit 1
          fi
          echo "Security gate passed."

  generate-dashboard:
    name: Generate Report Dashboard
    runs-on: ubuntu-latest
    needs: [security-gate]
    if: always()
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download unified findings
        uses: actions/download-artifact@v4
        with:
          name: unified-findings
          path: reports/

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Generate HTML dashboard
        run: python scripts/generate_report.py

      - name: Upload dashboard
        uses: actions/upload-artifact@v4
        with:
          name: dashboard
          path: reports/dashboard.html